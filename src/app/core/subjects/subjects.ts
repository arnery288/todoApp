import { Subject, BehaviorSubject, ReplaySubject, AsyncSubject } from 'rxjs'

// Standard subject
export const standardSubject = new Subject();

// Behavior subject
export const behaviorSubject = new BehaviorSubject<number>(0)

// Replay subject

// Async subject
